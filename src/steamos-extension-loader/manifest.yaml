name: "Extension Loader"
id: "loader"
description: "Core extension that persists other extensions between SteamOS updates (required)"
version: "1.0.0"
category: "system"
status: release
required: true

activation:
  mode: reboot
