name: "Tailscale VPN"
id: "tailscale"
description: "Persist Tailscale VPN across SteamOS updates"
version: "1.0.0"
category: "network"
release_status: release

activation:
  mode: auto

update_manager:
  script: "./update-manager"
